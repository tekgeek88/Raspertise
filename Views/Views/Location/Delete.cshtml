@model Raspertise.Models.Location

@{
    ViewData["Title"] = "Delete";
}

<div class="bg-primary m-1 p-1 text-white">
    <h1>Delete location</h1>
</div>

<div class="container h-100">
    <div class="row align-items-center p-5">
        <div class="col-6 mx-auto">
            <div class="card bg-dark rounded">
                <div class="card-title w-100">
                    <div class="bg-primary text-white w-100 p-2">
                        <h4>Are you sure you want to delete this?</h4>
                    </div>
                </div>
                <div class="card-body">
                    <h4 class="card-text text-light">Location</h4>
                    <table class="table table-dark table-striped-2 table-responsive w-auto table-bordered">
                        <thead>
                        <tr>
                            <td>@Html.DisplayNameFor(model => model.Description)</td>
                            <td>@Html.DisplayNameFor(model => model.PricePerDay)</td>
                        </tr>
                        </thead>
                        <tr>

                            <td>
                                @Html.DisplayFor(model => model.Description)
                            </td>
                            <td>
                                @Html.DisplayFor(model => model.PricePerDay)
                            </td>
                        </tr>

                    </table>
                    <form asp-action="Delete">
                        <input type="hidden" asp-for="AdvertiserId"/>
                        <input type="submit" value="Delete" class="btn btn-danger"/> |
                        <a asp-action="Index">Back to List</a>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>